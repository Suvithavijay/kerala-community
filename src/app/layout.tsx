import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import Navigation from "@/components/Navigation";
import AuthProvider from "@/components/AuthProvider";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "COSMA Community - Colorado Springs Malayali Community",
  description: "COSMA (Colorado Springs Malayali Community) brings together Malayali families in Colorado Springs to celebrate Kerala culture with fun, inclusiveness, and a feeling of being at home. Join our vibrant community for cultural events, traditional cuisine, and warm family connections.",
  keywords: "COSMA, Colorado Springs, Malayali, Kerala, community, events, culture, tradition, Colorado Springs Malayali Community",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={`${geistSans.variable} ${geistMono.variable} antialiased bg-black`}>
        <AuthProvider>
          <Navigation />
          <main className="min-h-screen">
            {children}
          </main>
        </AuthProvider>
      </body>
    </html>
  );
}
